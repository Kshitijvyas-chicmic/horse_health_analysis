# --- Horse Health API: PRODUCTION REQUIREMENTS (CPU Optimized) ---
# Use this file for the live server to keep the installation small and fast.
# Install via: pip install -r requirements.txt

# --- Core Web/API ---
fastapi
uvicorn
gunicorn
httpx
python-multipart
pydantic
pydantic-settings

# --- Computer Vision & ML (CPU-ONLY) ---
# This ensures we don't download 5GB of CUDA binaries
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.0.0
torchvision==0.15.1
torchaudio==2.0.1

# --- MMpose & OpenMMLab Core ---
mmcv==2.0.1
mmdet==3.3.0
mmengine==0.10.7
numpy==1.26.4 
opencv-python-headless>=4.8.0,<4.11.0
pillow==12.0.0
scipy

# --- Utilities ---
addict>=2.4.0
pyyaml
psutil
tqdm
